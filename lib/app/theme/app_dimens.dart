import 'package:flutter/material.dart';

class AppDimens {
  static const kSmallSpacing = 4.0;
  static const kSpacing = 8.0;
  static const kRadius = 10.0;
  static const kContentPadding = EdgeInsets.symmetric(
    vertical: kSmallSpacing,
    horizontal: kSpacing,
  );
  static const kAnimateDuration = Duration(milliseconds: 250);
  static const kButtonSize = Size(0, 44);
}
